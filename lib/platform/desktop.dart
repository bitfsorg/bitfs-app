/// Desktop platform (macOS / Windows / Linux) specific initialization.

/// Initialize desktop-specific features.
///
/// TODO: Set default window size (e.g. 1200x800),
/// configure native menu bar, register URI scheme handler
/// for bitfs:// deep links, set minimum window constraints.
Future<void> initDesktop() async {
  // TODO: implement desktop-specific initialization
}
